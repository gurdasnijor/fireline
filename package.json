{
  "name": "fireline",
  "version": "0.0.1",
  "private": true,
  "description": "Fireline runtime substrate for ACP-compatible agents (TypeScript workspace root)",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@fireline/client": "workspace:*",
    "@fireline/cli": "workspace:*",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.0.0"
}
