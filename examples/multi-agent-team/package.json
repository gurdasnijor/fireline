{
  "name": "multi-agent-team-example",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx --tsconfig ../tsconfig.json index.ts",
    "check": "tsc --noEmit --project ../tsconfig.json"
  },
  "devDependencies": {
    "tsx": "^4.20.6",
    "typescript": "^5.6.0"
  }
}
