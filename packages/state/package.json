{
  "name": "@fireline/state",
  "version": "0.0.1",
  "private": true,
  "description": "Fireline state schema (Zod + createStateSchema) and derived collection helpers (TanStack DB live queries)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc && pnpm emit-schema",
    "emit-schema": "tsx scripts/emit-schema.ts > dist/schema.json",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@durable-streams/state": "^0.2.3",
    "@tanstack/db": "^0.6.4",
    "zod": "^4.1.12",
    "zod-to-json-schema": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
