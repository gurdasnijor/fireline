{
  "name": "@fireline/cli",
  "version": "0.0.1",
  "private": true,
  "description": "Fireline CLI — run declarative agent specs locally",
  "type": "module",
  "bin": {
    "fireline": "./bin/fireline.js"
  },
  "files": [
    "bin",
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "pnpm run build && node --test dist/cli.test.js"
  },
  "dependencies": {
    "@fireline/client": "workspace:*",
    "tsx": "^4.20.6"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
