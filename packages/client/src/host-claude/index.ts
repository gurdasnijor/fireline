/**
 * Barrel for the Claude Agent SDK v2 Host satisfier module described in
 * `docs/proposals/client-primitives.md` §"Stress-test example — Claude
 * Agent SDK v2 host" and verified against the V2 surface in
 * `docs/explorations/claude-agent-sdk-v2-findings.md`.
 */
export * from './client.js'
